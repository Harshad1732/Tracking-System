{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "_NOTES": [
    "All sensitive values below MUST be overridden by Azure App Service > Configuration",
    "(or Key Vault references). Do not commit secrets here.",
    "",
    "App Settings keys (use '__' as the nested separator on Azure):",
    "  ConnectionStrings__Default     (Connection Strings tab; set Type=SQLAzure)",
    "  Jwt__Key                       (64+ random chars)",
    "  Cors__AllowedOrigins__0        (your frontend URL, e.g. https://app.example.com)",
    "  Email__Host, Email__Username, Email__Password, Email__FromAddress",
    "  Google__ClientId               (only if OAuth is enabled)",
    "  Microsoft__ClientId            (only if OAuth is enabled)"
  ],

  "ConnectionStrings": {
    "Default": ""
  },
  "Jwt": {
    "Key": ""
  },
  "Cors": {
    "AllowedOrigins": []
  },
  "Email": {
    "Host": "",
    "Port": 587,
    "UseSsl": true,
    "Username": "",
    "Password": "",
    "FromAddress": "",
    "FromName": "Tracker"
  }
}
